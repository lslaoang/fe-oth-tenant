server:
  error:
    path: /error
    whitelabel:
      enabled: false
spring:
  autoconfigure:
    exclude: org.springframework.boot.autoconfigure.web.ErrorMvcAutoConfiguration
  profiles:
    active: local